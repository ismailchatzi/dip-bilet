/** Scrappa Netlify'de yok — tarama VPS'te. */
export async function startScrappaWindow(_window: "full" | "near") {
  console.log("scrappa netlify kapalı — vps kullan");
}

export async function startScrappaDay(_opts?: { chunk?: number }) {
  console.log("scrappa netlify kapalı — vps start day");
}

export async function runScrappaTick(_force = false) {
  console.log("scrappa netlify kapalı — vps kullan");
}
