/** Scrappa Netlify cron — taramalar askıda. */
export async function startScrappaWindow(_window: "full" | "near") {
  console.log("taramalar askıda — scrappa start yok");
}

export async function runScrappaTick(_force = false) {
  console.log("taramalar askıda — scrappa tick yok");
}
