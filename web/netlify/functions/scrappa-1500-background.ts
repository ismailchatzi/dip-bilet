/** Eski near 15:00 — iptal; günlük near 07:00 start day içinde. */
export default async () => {
  console.log("scrappa-1500 iptal — start day (07:00) kullan");
};

export const config = {
  // VPS tarıyor; Netlify Scrappa kapalı
};
